# @package _global_

defaults:
  - override /synth: talnm

model:
  cfg: # 4_600_315 parameters
    _target_: models.preset.highway_ft
    hidden_features: 512
    num_blocks: 6
    block_kwargs: 
      norm: BatchNorm1d
      act_fn: ReLU
      dropout_p: 0.0
    embedding_kwargs:
      token_dim: 64
      pe_dropout_p: 0.0

task_name: talnm_highway_ft_b

ckpt_path: ${paths.ckpt_dir}/talnm_highway_ft_b_e7_mrr0.9779.ckpt