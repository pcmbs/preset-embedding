# @package _global_

defaults:
  - override /train_dataset: talnm_mn04_v1
  - override /val_dataset: talnm_mn04_v1
  - override /m_preset: mlp_gelu_oh


########## hyperparameters 

m_preset: # 27_249_600 parameters
  cfg:
    hidden_features: 2304
    num_blocks: 3

solver:
  lr: 5.99e-4
  optimizer:
    betas:
      - 0.847
      - 0.864
    eps: 2.208e-9

  scheduler:
    total_steps: 20000 # not an HP, must be (num_epochs * num_training_examples) / batch_size
    milestone: 7500
    final_lr: 4.095e-10

########## training setup
train_dataset:
  loader:
    batch_size: 512

trainer:
  # kwargs for lightning trainer
  max_epochs: 1
  
  # set True to to ensure deterministic results
  # makes training slower but gives more reproducibility than just setting seeds
  deterministic: True
  
    